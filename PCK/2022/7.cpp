#include<bits/stdc++.h>
using namespace std;

int main ()
{
    int n,m;
    cin >> n >> m;
    vector<int> t(m);
    vector<int> h(m);
    vector<bool> house(m);
    int sum_tax = 0;
    

    for (int i = 0; i < m; i++)
    {
        cin >> t[i] >> h[i];
    }

    
}